<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- JDBC connection -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/library_db?useSSL=false&amp;serverTimezone=UTC</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">Gowtham@2929</property>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Schema generation (update creates/updates tables) -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Show SQL in console -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Connection pool sizing (simple) -->
        <property name="hibernate.connection.pool_size">5</property>

        <!-- List of annotated entity classes -->
        <mapping class="com.library.model.User"/>
        <mapping class="com.library.model.Book"/>
        <mapping class="com.library.model.BookCopy"/>
        <mapping class="com.library.model.Transaction"/>
        <mapping class="com.library.model.Reservation"/>
    </session-factory>
</hibernate-configuration>
